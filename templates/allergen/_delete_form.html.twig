<form method="post" action="{{ path('app_allergen_delete', {'id': allergen.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ allergen.id) }}">
    <button class="btn">Supprimer un allergen</button>
</form>
